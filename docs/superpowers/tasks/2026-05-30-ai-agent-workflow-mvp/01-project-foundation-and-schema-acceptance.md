# 01 Project Foundation and Schema Acceptance

## Validation Commands

- `npm install`
- `npm run typecheck`
- `npm run test`
- `npm run lint`

## Manual Checks

- Confirm the project can start in development mode using the documented command.
- Confirm workflow schema types include the MVP node family from the spec.
- Confirm `.agentflow.json` validation rejects invalid version and malformed graph data.
- Confirm prompt variable utilities detect missing variables and resolve provided variables.
- Confirm no UI work beyond scaffold-level app shell was pulled into this task.

## Acceptance Result

Status: Not reviewed.

Record reviewer, date, command results, and manual review conclusion.

