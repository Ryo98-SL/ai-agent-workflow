# 02 Workbench and Canvas Acceptance

## Validation Commands

- `npm run typecheck`
- `npm run test`
- `npm run lint`

## Manual Checks

- Start the app and confirm the first screen is the workbench, not a landing page.
- Confirm the layout includes node palette, canvas, inspector, and debug panel.
- Confirm the user can add or select an LLM node and edit its prompt fields.
- Confirm node labels and controls fit without overlap at common desktop viewport sizes.
- Confirm non-LLM nodes are visible but do not imply unsupported real execution.
- Confirm this task did not add real model execution or file persistence.

## Acceptance Result

Status: Not reviewed.

Record reviewer, date, command results, and manual review conclusion.

