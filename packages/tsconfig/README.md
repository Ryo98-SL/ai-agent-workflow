# TypeScript Config

Shared TypeScript presets for workspace apps and packages.

Use a preset by extending it from a package `tsconfig.json`:

```json
{
  "extends": "@ai-agent-workflow/tsconfig/library.json"
}
```
