# Public Assets

## Purpose

Static browser assets served from the web app root by Vite.

## Files

- `favicon.png` is a cropped screenshot of the homepage header `AIW` product
  mark and is referenced from `index.html` as `/favicon.png`.
