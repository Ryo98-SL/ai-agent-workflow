# Workflow API Server

Minimal Hono server for the first workflow and deterministic mock run REST
endpoints.

```bash
pnpm --filter @ai-agent-workflow/server dev
```

The local server listens on `http://127.0.0.1:8788` by default. Set `PORT` to
override the port.
